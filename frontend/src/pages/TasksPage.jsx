import { Typography } from '@mui/material';

const TasksPage = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Мої задачі</Typography>
      <Typography variant="body1">Тут буде список завдань для онбордингу (додати документи, пройти курс тощо).</Typography>
    </div>
  );
};

export default TasksPage;